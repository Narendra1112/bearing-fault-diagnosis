# Core scientific computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Machine learning
scikit-learn>=1.3.0
imbalanced-learn>=0.11.0

# Deep learning  (CPU-only build; GPU variant auto-selected locally via extra-index-url)
torch>=2.0.0
torchvision>=0.15.0

# Signal processing & features
PyWavelets>=1.4.0
librosa>=0.10.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Dashboard
streamlit>=1.28.0

# Data handling
mat73>=0.60

# Utilities
tqdm>=4.65.0
joblib>=1.3.0

# API layer
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database (async SQLAlchemy)
SQLAlchemy>=2.0.0
aiosqlite>=0.19.0          # async SQLite driver (dev)
asyncpg>=0.29.0            # async PostgreSQL driver (prod)

# Monitoring
prometheus-client>=0.17.0

# ONNX runtime (edge inference + parity checks)
onnx>=1.15.0
onnxruntime>=1.16.0
onnxscript>=0.1.0          # required by torch.onnx.export (opset 17+)

# Experiment tracking
mlflow>=2.8.0

# API testing
requests>=2.31.0

# HF Hub (model download at startup in HF Spaces)
huggingface_hub>=0.20.0

# ── Dev / test only (not required at runtime) ────────────────────────────────
# pytest>=8.0.0
# httpx>=0.27.0
# locust>=2.40.0
